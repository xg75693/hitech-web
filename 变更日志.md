# 变更日志

## 2026-08-09 官网对话与后台功能优化

### 一、首页智能对话框

1. **欢迎语更新**：替换为含公司简介（2018 年成立、软件开发与 AI 智能体交付核心能力）的新版开场白。
2. **修复欢迎语丢失**：修复切换「透明工厂」后返回首页时，因组件重新挂载、从数据库加载历史而丢失开场欢迎语的问题——加载历史时在顶部补回欢迎语（带去重判断）。
3. **新增语音输入**：输入框新增麦克风按钮，基于浏览器 Web Speech API（zh-CN）实时语音转文字，可校对后发送；不支持的浏览器自动禁用并提示使用 Chrome/Edge。
4. **AI 回复风格优化**：寒暄后客户进入具象业务描述时，AI 回复切换为简明扼要、直击要点，避免冗长客套与重复话术。

### 二、留资（线索捕获）

1. **修复留资不产生**：原留资保存被 `chatState === "scoring"` 卡死，客户给完联系方式若不再发消息则永不保存；改为每轮收到 AI 回复后只要未保存即检测联系方式。
2. **联系方式识别放宽**：原正则仅认 11 位手机号，现支持座机、带「电话/号码/联系」上下文的 7-12 位号码、邮箱、微信号，并用上下文限定避免误匹配业务数字。
3. **后端按会话去重**：`POST /api/leads` 改为按 `session_id` upsert（已有则更新、无则插入），避免组件重新挂载后重复插入，`stats` 仅在新插入时 +1。

### 三、页面与后台

1. **footer 联系方式**：去掉 Twitter / LinkedIn / GitHub，改为企业邮箱 `chenqinsi@hitech.xin`。
2. **隐私政策 / 服务条款**：新增两个独立页面（`/privacy`、`/terms`），基于互联网 / AI 服务行业知识生成中文说明；footer 文字可点击进入。
3. **后台法律页面维护**：AdminPanel 新增「法律页面」tab，可编辑标题与正文（Markdown），保存即时生效。
4. **透明工厂**：去掉「内部访问权限 / 数字业务部」副标题。

### 四、数据库

- 新增 `legal_pages` 表（`page_key` / `title` / `content` / `updated_at`），存储隐私政策与服务条款，后台可维护。
- `schema.sql` 同步新增建表语句与初始数据；服务启动时 `initDatabase` 亦会自动建表并以 `INSERT IGNORE` 写入初始内容（已存在则保留后台编辑）。

### 涉及文件

- `server.ts`
- `src/App.tsx`
- `src/components/ChatInterface.tsx`
- `src/components/AdminPanel.tsx`
- `src/components/Dashboard.tsx`
- `src/components/LegalPage.tsx`（新增）
- `schema.sql`
